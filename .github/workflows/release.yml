name: Build and Release EXE

on:
  push:
    tags:
      - 'v*'

jobs:
  build-release:
    runs-on: windows-latest

    steps:
      # 1. ソースコード取得
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Pythonセットアップ
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3. 依存関係インストール & PyInstallerブートローダー独自ビルド
      - name: Install dependencies and rebuild PyInstaller bootloader
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install wheel
          git clone https://github.com/pyinstaller/pyinstaller.git
          cd pyinstaller/bootloader
          python ./waf distclean all
          cd ..
          pip install .

      # 4. 不要ファイルの削除（誤検知対策）
      - name: Clean old build artifacts
        run: |
          rmdir /S /Q build dist 2>nul || echo "No old build/dist"
          del /F /Q *.spec 2>nul || echo "No old spec"

      # 5. EXEビルド（クリーン・UPX無効・独自アイコン）
      - name: Build executable
        run: |
          pyinstaller --onefile --windowed `
            --name "Notion-File-Transfer" `
            --clean --noupx `
            --icon=assets/app.ico `
            main.py

      # 6. リリース作成 & EXE添付
      - name: Update Release with EXE and Notes
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: true
          files: ./dist/Notion-File-Transfer.exe
